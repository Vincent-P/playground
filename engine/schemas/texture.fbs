include "exo.fbs";
include "asset.fbs";
include "texture_header.fbs";

namespace engine.schemas;

table Texture {
  asset:Asset;
  format:PixelFormat;
  extension:ImageExtension;
  width:int;
  height:int;
  depth:int;
  levels:int;
  mip_offset:[ulong];
  pixels_data:[byte];
  data_size:ulong;
}

file_identifier "TXTR";
root_type Texture;