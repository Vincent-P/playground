doing
--
frame linear allocator for tmp format strings
allocator everything
--
todo
--
Use a LRU cache for staging buffers and free not used buffers @renderer
editor tooling: gizmo
asset browser ui, drag-n-drop
assets: free textures
assets: dont scan meta files at startup when importing all assets files
figure out how to integrate imgui properly instead of the UI::Context shit
window: use tagged union instead of std::variant
